<UserControl x:Class="Project_Ads.MVVM.View.BoardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Name="Board"
             d:DesignHeight="400" d:DesignWidth="500">
    
    <ListBox x:Name="advertisementsList"
        HorizontalAlignment="Stretch"
        HorizontalContentAlignment="Stretch">
        <ListBox.ItemTemplate>
            <DataTemplate>
                <Grid>
                    <Border VerticalAlignment="Center" 
                                    CornerRadius="15"
                                    BorderBrush="Black"
                                    BorderThickness="1"
                                    Padding="10"
                                    Margin="0,5,0,5">
                        <StackPanel Margin="5" Orientation="Horizontal">
                            <Border BorderBrush="#282828"
                                    BorderThickness="1"
                                    CornerRadius="30"
                                    Width="60"
                                    Height="60"
                                    VerticalAlignment="Center">
                                <Border.Background>
                                    <ImageBrush ImageSource="{Binding Path=ImageUrl}" Stretch="Fill"/>
                                </Border.Background>
                            </Border>
                            <DockPanel
                                Margin="10,0,0,0">
                                <TextBlock DockPanel.Dock="Top"
                                           FontWeight="Bold"
                                           FontSize="16" Text="{Binding Path=Title}" HorizontalAlignment="Left"/>
                                <TextBlock DockPanel.Dock="Bottom"
                                           Margin="0,10,0,0"
                                           FontSize="16" Text="{Binding Path=Description}" HorizontalAlignment="Left" />
                            </DockPanel>
                        </StackPanel>
                    </Border>
                </Grid>
            </DataTemplate>
        </ListBox.ItemTemplate>
    </ListBox>
</UserControl>
